# Circle-DailyWeekly

Ежедневные и еженедельные задания WoW Circle в отдельном окне.
**Questie не нужен.**

Если Questie у вас стоит, есть версия, которая показывает то же самое прямо в
его трекере: [QuestieCircle](https://github.com/sanyozeko/QuestieCircle).
Ставить оба аддона сразу не нужно — они делают одно и то же.

## Что умеет

- **Показывает, что задание можно взять** — по строке на группу, ежедневную и
  еженедельную. Выполненное гаснет и возвращается после сброса.
- **Помнит выполнение на весь аккаунт в пределах реалма.** Сдал на одном
  персонаже — видно на всех остальных этого же реалма. x1, x4, x100 и прочие
  считаются отдельно: это разные серверы со своими базами квестов.
- **Показывает, у кого лежит задание** — `Нардол - рейда: 3/5` под строкой.
- **Открывает меню по клику.** Не взято — нужный раздел `.menu`. Выполнено —
  сразу окно сдачи. У этих заданий нет NPC, только меню.
- **Напоминает** при входе в игру и раз в N минут. В бою молчит.
- **Кнопка у миникарты** всегда под рукой: клик — меню заданий,
  правый клик — настройки. Дополнительно можно включить значок или список.

## Как это выглядит

**Кнопка у миникарты есть всегда.** Она и показывает состояние значком, и
служит точкой входа: левый клик — меню заданий, правый — настройки. Наведение
показывает полное состояние. Тянется по кругу мышью.

Сверх этого можно включить ещё один способ показа:

| Дополнительно | Как ведёт себя |
|---|---|
| **Ничего** (по умолчанию) | Только кнопка у миникарты. |
| **Значок на экране** | Появляется, только когда есть незакрытое задание. Пропал — значит всё сделано. |
| **Список** | Строки в стиле трекера Questie: имя задания и прогресс. Шрифт, размеры, отступы и цвета взяты из исходников Questie. |

Переключить: правый клик по кнопке у миникарты, или
`/cdw style none|badge|list`. Скрыть саму кнопку — `/cdw minimap`.

## Установка

1. Скачать архив со страницы
   [Releases](https://github.com/sanyozeko/Circle/releases/latest)
   — папка внутри уже названа правильно.
2. Распаковать в `<папка игры>\Interface\AddOns\` так, чтобы получилось
   `Interface\AddOns\Circle-DailyWeekly\Circle-DailyWeekly.toc`
3. **Полностью перезапустить игру** — новую папку аддона `/reload` не подхватит.

Проверить: `/cdw status` в игре.

## Команды

| Команда | Что делает |
|---|---|
| `/cdw` | показать или скрыть окно |
| `/cdw status` | состояние, кто что держит, время сбросов |
| `/cdw take` | открыть меню для того, что доступно |
| `/cdw daily` / `/cdw weekly` | открыть конкретный раздел |
| `/cdw done daily` | отметить выполненным вручную |
| `/cdw undone weekly` | снять отметку |
| `/cdw remind 30` | напоминать раз в 30 минут (по умолчанию выключено) |
| `/cdw sound` | звук напоминания (по умолчанию выключен) |
| `/cdw testsound` | проиграть звук, чтобы проверить |
| `/cdw lock` | закрепить на месте |
| `/cdw style badge` | дополнительный вид: `none`, `badge`, `list` |
| `/cdw minimap` | показать или скрыть кнопку у миникарты |

Длинный вариант — `/circledw`. Команду `/circle` аддон намеренно не
занимает: её регистрирует версия для Questie, и они бы конфликтовали.

Ручная отметка нужна редко: если игра упала сразу после сдачи или задание сдали
ещё до установки аддона. Сервер не сообщает о сданных **ежедневных** заданиях
(о еженедельных — сообщает), поэтому восстановить такое автоматически нельзя.

## Звук

Своя короткая нарезка в `Sounds/alert.mp3`. Формат подобран под старый
звуковой движок 3.3.5a: MPEG-1 Layer III, 44100 Гц, 128 кбит/с, без ID3-тега —
ровно как у файлов DBM, которые в этом клиенте заведомо работают. Если файл
не проиграется, аддон возьмёт стандартный звук Raid Warning.

Заменить — положить свой `alert.mp3` в ту же папку. Проверить — `/cdw testsound`.
Звук по умолчанию выключен, включается `/cdw sound` или галочкой в настройках.

## Настройка

Список заданий — таблица `ns.QUESTS` вверху `Core.lua`.
День недельного сброса — `WEEKLY_RESET_WDAY` там же (сейчас среда).

## Ограничения

- Срез персонажа пишется при выходе или `/reload`; персонажи, которыми не
  заходили после установки, невидимы.
- Ежедневный сброс берётся с сервера, недельный вычисляется от него по дню
  недели. Если сервер сменит расписание, поправьте константу.
